name: methylation_bismark
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  # Core bisulfite sequencing tools
  # Note: cutadapt, trim_galore, samtools installed in base env to avoid conda conflicts
  - python>=3.9
  - bismark>=0.24
  - bowtie2>=2.5
  - fastqc>=0.12
