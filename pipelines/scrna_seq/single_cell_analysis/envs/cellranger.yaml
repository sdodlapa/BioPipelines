name: scrna_cellranger
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - python>=3.9
  # Note: CellRanger and h5py installed separately to avoid libcurl conflicts
  # Install with: pip install h5py pandas numpy
  # - h5py>=3.7
  - pandas>=1.5
  - numpy>=1.23
