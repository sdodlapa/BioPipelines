name: sv_calling
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - python>=3.9
  # SV merging tool
  - survivor>=1.0
  # Note: Manta removed (requires Python 2.7)
  # Note: Delly removed (libdeflate/htslib version conflicts)
  # Note: Using LUMPY from base env + samtools + Python for SV calling
  # Note: samtools, bcftools, tabix from base env
