name: sv_calling
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - python>=3.9
  # SV callers - install via conda
  - delly>=1.1
  - manta>=1.6
  # SV merging
  - survivor>=1.0
  # Note: samtools, bcftools, tabix from base env (LUMPY uses samtools)
