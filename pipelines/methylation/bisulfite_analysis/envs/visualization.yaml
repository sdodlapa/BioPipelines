name: methylation_viz
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  # Simplified visualization (no strict pinning)
  - python=3.9
  - matplotlib
  - seaborn
  - pandas
  - numpy
  - scipy
