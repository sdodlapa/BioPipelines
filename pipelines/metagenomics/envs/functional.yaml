name: metagenomics_functional
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  # Skip humann for now - install separately with pip if needed
  # Functional profiling tools
  - diamond>=2.0
  
  # Utilities
  - python>=3.9
  - pandas
  - matplotlib
  - biopython>=1.79
