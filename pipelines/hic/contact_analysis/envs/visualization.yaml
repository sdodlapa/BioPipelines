name: hic_visualization
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  # Hi-C visualization tools (ultra-minimal)
  - cooler>=0.9
  - hicexplorer>=3.7
  - cooltools>=0.5
  - pygenometracks>=3.8
  - python>=3.10
  - numpy
  - jinja2
  # Note: matplotlib, seaborn, pandas, plotly from base env
