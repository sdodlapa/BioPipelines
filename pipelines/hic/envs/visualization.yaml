name: hic_visualization
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  # Minimal Hi-C tools
  - cooler>=0.9  # Contact matrix operations
  - python>=3.10
  - numpy
  - jinja2
  # Note: matplotlib, seaborn, pandas from base env
  # Note: hicexplorer, cooltools, pygenometracks removed (unsolvable dependency conflicts)
  # Note: Use cooler + matplotlib for visualization instead
